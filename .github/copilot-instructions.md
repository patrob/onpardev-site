Always run command `nvm use` before running any Node.js commands to ensure the correct version is used.
Always ensure running the correct version of Node.js by checking with `node -v` after running `nvm use`.
Always use the same terminal session for running commands after `nvm use` to avoid version conflicts.
