---
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="text-content">
        <h1>Expert Software Consulting That Delivers Results</h1>
        <p class="subtitle">Transform your technical challenges into competitive advantages with proven expertise in architecture, development, and team coaching.</p>
        <div class="cta-buttons">
          <a href="#contact" class="btn">Book a Free Consultation</a>
          <a href="#services" class="btn-outline">Explore Services</a>
        </div>
      </div>
      <div class="hero-image">
        <div class="hero-visual">
          <div class="code-window">
            <div class="window-header">
              <div class="window-controls">
                <span class="control red"></span>
                <span class="control yellow"></span>
                <span class="control green"></span>
              </div>
              <div class="window-title">software-consulting.js</div>
            </div>
            <div class="code-content">
              <div class="code-line"><span class="keyword">const</span> <span class="variable">consulting</span> = &#123;</div>
              <div class="code-line">  <span class="property">expertise</span>: <span class="string">'architecture'</span>,</div>
              <div class="code-line">  <span class="property">delivery</span>: <span class="string">'on-time'</span>,</div>
              <div class="code-line">  <span class="property">results</span>: <span class="string">'guaranteed'</span></div>
              <div class="code-line">&#125;;</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <div class="hero-background"></div>
</section>

<style>
  .hero {
    position: relative;
    padding: var(--space-24) 0 var(--space-16);
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
  }
  
  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
      linear-gradient(135deg, var(--base-50) 0%, var(--primary-50) 50%, var(--accent-50) 100%);
    z-index: -1;
  }
  
  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-8);
    align-items: center;
  }
  
  .text-content {
    max-width: 540px;
  }
  
  h1 {
    font-size: 3.5rem;
    line-height: 1.1;
    margin-bottom: var(--space-6);
    background: linear-gradient(90deg, var(--text-900), var(--primary-700), var(--accent-700));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  
  .subtitle {
    font-size: 1.25rem;
    line-height: 1.5;
    color: var(--text-700);
    margin-bottom: var(--space-8);
  }
  
  .cta-buttons {
    display: flex;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
  }
  
  .btn-outline {
    display: inline-block;
    padding: var(--space-3) var(--space-6);
    border: 2px solid var(--primary-600);
    color: var(--primary-600);
    border-radius: var(--radius-md);
    font-weight: 500;
    cursor: pointer;
    transition: all var(--transition-normal) ease;
    text-align: center;
  }
  
  .btn-outline:hover {
    background-color: var(--primary-50);
    border-color: var(--primary-700);
    color: var(--primary-700);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }
  
  .hero-image {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }
  
  .hero-visual {
    width: 100%;
    max-width: 500px;
  }
  
  .code-window {
    background: var(--text-900);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    overflow: hidden;
    transform: perspective(1000px) rotateY(-5deg);
    transition: transform var(--transition-normal) ease;
  }
  
  .code-window:hover {
    transform: perspective(1000px) rotateY(0deg);
  }
  
  .window-header {
    background: var(--text-800);
    padding: var(--space-3) var(--space-4);
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid var(--text-700);
  }
  
  .window-controls {
    display: flex;
    gap: var(--space-2);
  }
  
  .control {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }
  
  .control.red {
    background: #ff5f57;
  }
  
  .control.yellow {
    background: #ffbd2e;
  }
  
  .control.green {
    background: #28ca42;
  }
  
  .window-title {
    color: var(--base-300);
    font-size: 0.875rem;
    font-weight: 500;
  }
  
  .code-content {
    padding: var(--space-6);
    font-family: 'SF Mono', 'Monaco', 'Inconsolata', 'Roboto Mono', monospace;
    font-size: 0.875rem;
    line-height: 1.6;
  }
  
  .code-line {
    color: var(--base-200);
    margin-bottom: var(--space-2);
  }
  
  .keyword {
    color: var(--secondary-400);
    font-weight: 600;
  }
  
  .variable {
    color: var(--primary-300);
  }
  
  .property {
    color: var(--accent-400);
  }
  
  .string {
    color: var(--primary-200);
  }
  
  @media (max-width: 1024px) {
    h1 {
      font-size: 3rem;
    }
  }
  
  @media (max-width: 768px) {
    .hero {
      padding-top: 120px;
    }
    
    .hero-content {
      grid-template-columns: 1fr;
      gap: var(--space-10);
    }
    
    .text-content {
      max-width: 100%;
      text-align: center;
    }
    
    h1 {
      font-size: 2.5rem;
    }
    
    .cta-buttons {
      justify-content: center;
    }
  }
  
  @media (max-width: 640px) {
    h1 {
      font-size: 2rem;
    }

    .subtitle {
      font-size: 1.125rem;
    }

    .cta-buttons {
      flex-direction: column;
      width: 100%;
    }

    .btn, .btn-outline {
      width: 100%;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding-top: 100px;
    }

    .hero-image {
      margin-top: var(--space-6);
    }
  }
</style>